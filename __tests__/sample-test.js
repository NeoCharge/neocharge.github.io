import React from 'react';
import Intro from '../neocharge-app/components/OnboardingInput';

import renderer from 'react-test-renderer';
import expectExport from 'expect';

test('returns true', () => {
  expect(true, true);
});